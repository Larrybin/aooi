---
task: 仓库三维体检报告（可维护性/复用性/耦合度）
mode: 执行计划存档
scope:
  include:
    - src/**
    - scripts/**
    - package.json
    - tsconfig.json
    - eslint.config.mjs
    - next.config.mjs
    - README.md
  exclude:
    - raphael-starterkit-v1-main/**
    - .next/**
    - node_modules/**
principles: [SOLID, KISS, DRY, YAGNI]
validation:
  - 静态扫描与依赖方向审计
  - 关键模块抽样深读（payment/rbac/auth/config）
deliverable:
  - 三维评分、证据点、风险分级、改进建议（ROI 排序）
---

## 执行步骤

1. 固化范围与扫描基线（排除 raphael-starterkit-v1-main/.next/node_modules）。
2. 结构与职责边界盘点：按 app/core/shared/extensions/themes/scripts 归类模块职责与入口/出口。
3. 耦合度审计：统计与检查跨层 import（依赖方向、反向依赖、潜在循环、扇入扇出热点）。
4. 可维护性审计：按文件体积/分支密度/导出密度筛热点，重点复核支付/RBAC/auth/config。
5. 复用性审计：检查 UI blocks/themes 与 services/models 的抽象复用，识别重复模式与可抽象点。
6. 汇总报告：给出三维评分、问题清单、风险等级与可执行改进建议（按 ROI）。

