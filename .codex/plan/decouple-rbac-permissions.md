# 任务：下沉 PERMISSIONS 常量并迁移引用\n\n## 目标\n- 将 PERMISSIONS 从 src/shared/services/rbac_guard.ts 下沉到纯常量模块 src/shared/constants/*。\n- 逐步迁移仓内引用点到新常量模块，减少仅为取常量而导入 guard 的耦合。\n- 保持兼容：src/shared/services/rbac_guard.ts 仍导出 PERMISSIONS（对外 API 不破坏）。\n\n## 约束\n- 行为不变：所有权限 code 字符串保持一致。\n- 不新增 env。\n- 遵循 Next.js 最佳实践：避免 client 误导入 server-only 逻辑（通过拆分纯常量模块实现）。\n\n## 步骤\n1) 新增常量模块\n   - 新建 src/shared/constants/rbac-permissions.ts\n   - 导出：xport const PERMISSIONS = { ... } as const;（内容与当前 bac_guard.ts 完全一致）。\n   - 预期：该文件无 server-only、无副作用 import。\n\n2) 保持兼容导出\n   - 更新 src/shared/services/rbac_guard.ts：\n     - 删除原 PERMISSIONS 字面量定义\n     - 改为 import { PERMISSIONS } from '@/shared/constants/rbac-permissions'\n     - 保持 xport { PERMISSIONS }（或直接 re-export）\n   - 预期：bac_guard 对外 API 不变，引用点可继续使用旧路径。\n\n3) 迁移仓内引用点（本轮完成）\n   - 将以下文件中 PERMISSIONS 的 import 从 @/shared/services/rbac_guard 改为 @/shared/constants/rbac-permissions：\n     - src/app/api/user/get-user-info/route.ts\n     - 所有 src/app/[locale]/(admin)/admin/**/page.tsx 中仅使用 PERMISSIONS + guard 函数的：保留 guard 函数从 bac_guard 导入，PERMISSIONS 单独从 constants 导入。\n   - 更新 src/core/rbac/permission.ts：改为 re-export 新常量模块与/或保留现有 re-export 但不额外引入 guard（视当前用途）。\n   - 预期：页面仍使用 guard 函数，但常量不再导致额外依赖链。\n\n4) 回归验证\n   - 
px tsc --noEmit\n   - pnpm build\n   - 预期：类型检查与构建通过；无运行时行为变化。\n\n## 风险与回滚\n- 风险：漏改 import 导致仍从 bac_guard 引入常量（但兼容导出存在，功能不受影响）。\n- 回滚：可通过 git checkout -- <file> 回退本次变更。\n
