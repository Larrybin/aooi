# Background Remover

Background Remover is a standalone SaaS site instance keyed by
`background-remover`. The app uses Cloudflare Images `segment=foreground` to
produce transparent PNG output.

Operational docs:

- [Preview deployment](./preview-deployment.md)
